import Input, {InputText} from './input/index';
import {Select} from './select/index';
import Control from './control/index';

export {Input, InputText, Select, Control}
export default {Input, InputText, Select, Control}